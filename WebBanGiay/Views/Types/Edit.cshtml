@model WebBanGiay.Models.Type

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="jumbotron py-4 text-center">
    <h1>Chỉnh sửa loại sản phẩm</h1>
</div>

<div class="row justify-content-center">
    <div class="col-md-6">
        <form asp-action="Edit" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TypeId" />

            <div class="form-group">
                <label asp-for="TypeName" class="control-label">Tên loại</label>
                <input asp-for="TypeName" class="form-control" />
                <span asp-validation-for="TypeName" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="TypeImage" class="control-label">Hình ảnh loại</label>
                <input asp-for="TypeImage" class="form-control" />
                <span asp-validation-for="TypeImage" class="text-danger"></span>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary btn-block">Lưu</button>
            </div>
        </form>
    </div>
</div>

<div class="row justify-content-center">
    <div class="col-auto">
        <a asp-action="Index" class="btn btn-secondary">Quay lại danh sách</a>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
